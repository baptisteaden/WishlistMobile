import React from 'react';
import { withTheme } from 'react-native-paper';
import { withProps } from '../_common/_helpers';

const WishConsult: () => React$Node = ({ theme, username }) => {
  return <>bonsoir</>;
};

export default withTheme(WishConsult);
